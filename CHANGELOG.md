# Changelog

## 1.2.21-alpha

- Интро снова работает: по умолчанию без `-connect` (автоподключение — опция в настройках)
- Пароль сервера по умолчанию: `sdfhjgds`
- Фикс `noise_raw.paa` от прямого входа мимо меню

## 1.2.20-alpha

- Фикс зависания Arma в фоне: удалён `steam_appid.txt`, убран `-hugePages`/`windowsHide`
- Perf-флаги только при «Оптимизированный запуск»; перед стартом поднимается Steam

## 1.2.19-alpha

- Убрана сборка/встройка `@SF_CHAR_MENU` (EBUSY + краш при запуске)
- BattlEye всегда включён при входе на сервер (убран `-noBattlEye`)

## 1.2.18-alpha

- Фикс запуска Arma: снова прямой старт exe (Steam `-applaunch` не поднимал игру)
- Пароль TeamSpeak в настройках — подставляется при подключении к TS

## 1.2.17-alpha

- Фикс интро / `noise_raw.paa`: убраны `-skipIntro`/`-world=empty`/`-noSplash` из оптимизированного запуска; запуск через Steam
- Оптимизированный запуск: только profiling + CPU/RAM, интро и логотипы сохраняются
- Пароль сервера в настройках передаётся в `-password` при подключении

## 1.2.16-alpha

- Автообновление: проверка через `latest.yml` (без лимита GitHub API)

## 1.2.15-alpha

- Holonet: длинный текст новости снова прокручивается (не обрезается)
- В настройках — постоянный **ID клиента** установки (копирование)
- Discord: `/add_star_point` (техсостав) — зачисление STAR POINT по ID клиента

## 1.2.14-alpha

- Интро меню нельзя отключить: убраны переключатели «Пропускать интро» и «Статический фон»; флаги `-skipIntro` / `-world=empty` больше не передаются
- Обновлён встроенный пресет модов: **SF4 (6.09)**
- Меню персонажа снова вшито в лаунчер (runtime `@SF_CHAR_MENU`)

## 1.2.13-alpha

- YouTube в Holonet/новостях: фикс Error 153 (Referer для Electron file://) + кнопка «Открыть на YouTube»

## 1.2.12-alpha

- Персонажи: список в настройках, выбор активного, переверификация и доп. персонаж
- Holonet / Новости: просмотр видео (вложения Discord, YouTube) прямо в лаунчере

## 1.2.11-alpha

- Holonet: картинка-заглушка `holonews.jpg`, кэш лент без мигания
- Звуки кнопок (hover/click)
- Новости/Holonet: добор через отдельные API + сохранение предыдущих данных
- Пресет модов: SF4 от 05.09.2026

## 1.2.10-alpha

- Верификация: бот больше не ставит ✅ сам (только модерация вручную)
- CR = ВАР; преписка подразделения → доп. одобрение командира
- После одобрения автоматически создаётся профиль Arma; без долгой блокировки входа

## 1.2.9-alpha

- Верификация персонажа: заявка из гайда → канал Discord, одобрение ✅
- CR: кнопка «Вступить в подразделение», форма заявки, назначение звания/должности командиром
- Профиль: фракция/звание из верификации; запрос ролей в канал модерации
- Оптимизация: опрос онлайна 8с, фракция больше не форсится в «ВАР»

## 1.2.5-alpha

- Поддержка: исправлены тикеты/предложения/заявки (OAuth Discord, ошибки API)
- API: таймауты и обработка ошибок (ЮMoney, тикеты)
- Лаунчер: сообщения об ошибках вместо «мёртвых» кнопок

## 1.2.4-alpha

- Исправлен синий экран при запуске (восстановлен `window.rimLauncher`)
- Онлайн: выбор лучшего источника (A2S 109.248.4.45:2303, online-bot, API бота)

## 1.2.2-alpha

- Переименование: **STAR POINT** вместо RIM POINT

## 1.2.0-alpha

- Ребрендинг лаунчера: **StarFront**
- Новый фон (StarFront artwork)

## 1.1.7-alpha

- Исправлено начисление RIM POINT за игру (Rim API playtime + fallback)

## 1.1.4-alpha

- Обновлён встроенный пресет модов (StarFront, 07.06.2026)

## 1.1.3-alpha

- Окно обновления при выходе новой версии — скачивание и установка прямо из лаунчера
- Исправлена проверка обновлений для pre-release релизов на GitHub
- Прямые ссылки на скачивание EXE (без битого `/releases/latest/download/`)
- TeamSpeak: пропуск повторного подключения, если уже на сервере
- Rim points: лаунчер остаётся в фоне во время игры

## 1.1.0-alpha

- Обновлён встроенный пресет модов (StarFront, 06.06.2026)
- Авто-проверка обновлений через GitHub Releases (`latest.yml`)
- GitHub Actions: сборка EXE + ZIP на каждый тег `v*`

## 1.0.0

- Первый публичный релиз
- Discord OAuth, пресет модов, TeamSpeak + TFAR
- Онлайн сервера, RIM POINT за время в игре
